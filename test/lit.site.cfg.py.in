@LIT_SITE_CFG_IN_HEADER@
import os

config.tensorforge_src_dir = path(r"@TENSORFORGE_SOURCE_DIR@")
config.tensorforge_obj_root = path(r"@TENSORFORGE_BINARY_DIR@")
config.llvm_tools_dir = path(r"@LLVM_TOOLS_DIR@")
config.tensorforge_tools_dir = path(r"@TENSORFORGE_TOOLS_DIR@")

import lit.llvm

lit.llvm.initialize(lit_config, config)
lit_config.load_config(
    config, os.path.join(config.tensorforge_src_dir, "test/lit.cfg.py")
)
